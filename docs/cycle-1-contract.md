# Контракт цикла 1 — генерация лидов

**Репозиторий:** `evorove_lead`  
**Якорь продукта:** `/Users/alenakulish/dev/evorove/FOUNDATION.md`  
**Этап:** `/Users/alenakulish/dev/evorove/docs/foundation-alignment-stage-ru.md`  
**Карта трёх репо:** `/Users/alenakulish/dev/evorove/docs/three-repos-next-steps-ru.md`

Этот файл — контракт **цикла 1**. Контракт горячего лида (выход продажи → вход CRM) ему не принадлежит: это стык циклов 2→3.

Сейчас это каркас и проверка оффера по файлам владельца. Поиска людей в прод нет. Владелец не должен считать репозиторий живой генерацией.

---

## Вход: материалы, которые положил владелец

Агент **не ищет** объявления в чужих кабинетах и **не скрейпит** чужие или платные рекламные поверхности.

Владелец сама кладёт файлы в [`owner-materials/`](../owner-materials/). Допустимый вход:

- её текст объявления (копия, которую она имеет право отдать);
- URL **её** сайта или лендинга;
- её описание услуг своими словами.

Не вход этого среза:

- чужие объявления, снятые скрейпом;
- платный/закрытый контент;
- секреты, ключи, `.env`;
- подключение Bing / Google / Meta Ads по OAuth (владелец введёт секреты сама, отдельным шагом, не сейчас).

---

## Понимание оффера и аудитории

Из положенных **файлов** получается структурированное понимание. Этот срез **не вызывает LLM** и **не открывает URL**: он принимает предложенное понимание, только если каждое утверждение уже есть в тексте, который владелец положила.

| Поле | Смысл |
| --- | --- |
| **what_we_sell** | Что продаём — цитата из материала, без домысла |
| **who_may_fit** | Кому это может подойти — тоже из материала |
| **commercial_claims** | Цена / скидка / гарантия / юридическое утверждение — только если они уже написаны в материале |
| **must_not_promise** | Остальные коммерческие виды: агент не имеет права их обещать |

Запрещено выдумывать цену, скидку, гарантию и юридическое утверждение, которых нет в материалах владельца. URL сайта, положенный в файл, — указатель, не повод сходить в сеть. Отраслевых вилок в коде нет: юриспруденция — не продукт, а возможный вход на рынок.

Business DNA из репозитория продажи не копировать. Здесь только факты, нужные поиску: оффер и аудитория из файлов владельца.

Пока в `owner-materials/` нет её копирайта (один README — не материал), оффера нет. Это не живая генерация.

Инвариант в коде: `src/evorove_lead/offer.py`, загрузка файлов: `src/evorove_lead/materials.py`.

---

## Выход: кандидат

Результат цикла — не «база контактов». Запись допустима только если есть все три поля:

| Поле | Смысл |
| --- | --- |
| **identity** | Личность, которую цикл 2 **потом** сможет адресовать (не отправка сейчас) |
| **reason** | Почему этот человек здесь, привязка к офферу/аудитории |
| **reason_source** | Откуда взялась причина (какой материал, какой наблюдаемый факт) |

Запись **без причины отклоняется**. Контакта мало. Пустая или пробельная причина — не кандидат.

Это ещё не продажа, не заполненная CRM-карточка и не слот в календаре.

Инвариант в коде (без поиска людей): `src/evorove_lead/candidate.py`. Причина кандидата должна быть привязана к понятому офферу (`accept_candidate_for_offer`): контакта мало, «есть телефон» — не причина.

### Handoff 1→2 (когда появится)

Найденный человек + причина + источник + канал, которым цикл 2 потом можно писать. Письмо и GREET — не здесь. Пока между репозиториями нет очереди/API — не делать вид, что контуры уже склеены.

Горячий лид (согласен на услугу, готов записаться, ещё не стоит в календаре) — **не** этот контракт.

---

## Явные не-цели

Цикл 1 **не** делает:

- исходящее письмо, SMS, звонок, очередь «написать на этот телефон»;
- разговор продажи, возражения, GREET, цитаты цены;
- календарь, бронь, смету, `ProcessEngine`;
- виджет или intake «нам уже написали» как генерацию;
- поиск людей, обогащение, скрейп LinkedIn — не на этом срезе;
- `SalesStage` / `ProcessState` (это продажа и обязательства кейса);
- Lemon Squeezy, Twilio, встраиваемый чат.

---

## Рекламные библиотеки и кабинеты (не этот срез)

| Источник | Статус |
| --- | --- |
| Материалы, которые владелец положила сама | Вход цикла 1 |
| Microsoft Ad Library API | Публичный, но **только показы в EEA** (DSA). Не источник истины для рынка США / SMB. Позже — **опциональный** исследовательский вход, не ядро и не этот срез |
| Google Ads Transparency Center | Не скрейпить внутренности |
| Bing / Google / Meta Ads (свой кабинет) | Отдельный шаг с OAuth; секреты вводит владелец, агент их не просит и не вставляет |

---

## Что цикл 1 всё ещё не умеет

- Сам «прочитать объявление и понять» без предложенных цитат: нет LLM и нет извлечения.
- Открыть URL сайта владельца и снять с него текст.
- Находить людей.
- Писать им.
- Подключать рекламные кабинеты.
- Передавать кандидата в `evorove` по событию/API.
- Обещать клиенту, что «система уже находит клиентов».
